/**
 * Profile Generator – feature barrel export
 *
 * Re-exports the pages so the router can import them cleanly:
 *   import { ProfileListPage, ProfileEditorPage } from './features/profile-generator';
 */

export { ProfileListPage } from './ProfileListPage';
export { ProfileEditorPage } from './ProfileEditorPage';
